numpy>=1.24
opencv-python-headless>=4.8
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
SQLAlchemy>=2.0
alembic>=1.13

# Optional — Postgres driver (set DATABASE_URL=postgresql+psycopg://...):
# psycopg[binary]>=3.1
# Optional — S3 object storage (PLAYMETRICS_STORAGE=s3):
# boto3>=1.34
# Optional — Redis-backed job queue (PLAYMETRICS_QUEUE=rq):
# rq>=1.16
# redis>=5.0
# Optional — enables the real YOLO detector backend (downloads PyTorch):
# ultralytics>=8.1
