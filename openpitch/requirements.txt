numpy>=1.24
opencv-python-headless>=4.8
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
SQLAlchemy>=2.0
alembic>=1.13

# Optional — Postgres driver (set DATABASE_URL=postgresql+psycopg://...):
# psycopg[binary]>=3.1
# Optional — enables the real YOLO detector backend (downloads PyTorch):
# ultralytics>=8.1
