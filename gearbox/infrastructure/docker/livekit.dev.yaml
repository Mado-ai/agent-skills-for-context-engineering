# Development-only LiveKit configuration. These keys are intentionally well-known and
# must never appear in any deployed environment.
port: 7880
rtc:
  tcp_port: 7881
  port_range_start: 50000
  port_range_end: 50100
  use_external_ip: false
keys:
  devkey: devsecretdevsecretdevsecretdevsecret
logging:
  level: info
